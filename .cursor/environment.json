{
  "name": "Quant Stock Analytics",
  "install": "pip install --user -r requirements.txt --quiet",
  "terminals": [
    {
      "name": "streamlit",
      "command": "export PATH=\"$HOME/.local/bin:$PATH\" && streamlit run app.py --server.port 8741 --server.address 0.0.0.0 --server.headless true"
    }
  ],
  "ports": [
    { "name": "streamlit", "port": 8741 }
  ]
}
